server:
  port: 8103
spring:
  application:
    name: audit-service
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/pharmacy_audit
    username: root
    password: 160525
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: false
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
