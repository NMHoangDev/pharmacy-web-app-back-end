server:
  port: 7027

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pharmacy_consultation?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

consultation:
  pharmacist:
    base-url: http://localhost:8087

logging:
  level:
    root: INFO
